On server `public/css/shop.css`, replace:

`.admin-side a:hover, .admin-side a.active { background: rgba(255,255,255,0.08); color: white; }`

so submenu links under `.adm-sub` keep dark readable text (not white-on-light).
